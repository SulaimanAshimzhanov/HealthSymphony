

import React from 'react';

const AuthSubmit = ({location}) => {
  return (
    <React.Fragment>
        <button type='submit'>
            {location}
        </button>
    </React.Fragment>
  )
}

export default AuthSubmit;
